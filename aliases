# Unix
alias mkdir='mkdir -p'
alias ll='ls -l'
alias la='ls -al'

# Vim
alias vi='vim'

# Git
alias g='git status'

# Tmux
alias tmux='EDITOR= VISUAL= tmux'

# Include custom aliases
[[ -f .aliases.local ]] && source ~/.aliases.local
